import { HomeComponent } from './../home/home.component';
import { RatesDirective } from './rates.directive';



describe('RatesDirective', () => {
  it('should create an instance', () => {
    const directive = new RatesDirective();
    expect(directive).toBeTruthy();
  });
});
